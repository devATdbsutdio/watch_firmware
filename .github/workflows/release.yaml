name: Tagged-release

on:
  workflow_dispatch:

# jobs:
#   tagged-release:
#     name: Tagged Release
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write
#       contents: write

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2

#       - name: Release
#         uses: softprops/action-gh-release@v1
#         if: startsWith(github.ref, 'refs/tags/')
#         with:
#           # token: "${{ secrets.RELEASE_ACCESS_TOKEN }}"
#           token: ${{ github.token }}
#           draft: false
#           prerelease: false
#           name: "v1.0.0"
#           tag_name: "v1.0.0"
#           fail_on_unmatched_files: true
#           files: |
#             $(pwd)/build/*.hex
#         env: 
#           GITHUB_REPOSITORY: devATdbsutdio/watch_firmware


jobs:
  build_&_upload_release:
    name: build_&_upload_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: create release artifact
        uses: actions/create-release@v1
        id: create_release
        with:
          draft: false
          prerelease: false
          release_name: 'v1.0.0'
          tag_name: 'v1.0.0'
        env:
          GITHUB_TOKEN: ${{ github.token }}


      - name: set hex file's name and path as env vars
        run: |
          echo "HEX_FILE_NAME=$(cd $(pwd)/build && ls *.hex)" >> $GITHUB_ENV
          echo "HEX_FILE_PATH=./build/$(cd $(pwd)/build && ls *.hex)" >> $GITHUB_ENV
      

      - name: /[SANITY CHECK/] hex file's name and hex file's path as read from env vars for upload step
        run: |
          echo "${{ env.HEX_FILE_NAME }}"
          echo "${{ env.HEX_FILE_PATH }}"


      - name: upload release artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ env.HEX_FILE_PATH }}
          asset_name: ${{ env.HEX_FILE_NAME }}
          asset_content_type: application/hex